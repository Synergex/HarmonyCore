import System
import System.Collections.Generic
import System.Text
import System.Threading.Tasks


namespace Harmony.Core.Context

	public interface IDynamicCallConnection
		method WriteRaw, @Task
			rawData, [#]byte
		endmethod

		method ReadRaw, @Task<string>
			terminator, char
		endmethod

        method MakeReady, @Task
        endmethod

		method Disconnect, void
		endmethod

		method Recycle, @Task
		endmethod
	endinterface

endnamespace
