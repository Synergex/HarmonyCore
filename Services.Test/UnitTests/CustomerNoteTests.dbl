;;*****************************************************************************
;;
;; Title:       CustomerNoteTests.dbl
;;
;; Description: Unit tests for the operations defined in CustomerNotesController.
;;
;;*****************************************************************************
;; WARNING: GENERATED CODE!
;; This file was generated by CodeGen. Avoid editing the file if possible.
;; Any changes you make will be lost of the file is re-generated.
;;*****************************************************************************

import Microsoft.AspNetCore.JsonPatch
import Microsoft.VisualStudio.TestTools.UnitTesting
import Newtonsoft.Json
import System.Collections.Generic
import System.Net
import System.Net.Http
import Services
import Services.Test.Models

namespace Services.Test.UnitTests

    {TestClass}
    public partial class CustomerNoteTests

        ;;------------------------------------------------------------
        ;;Get all CustomerNotes

        {TestMethod}
        {TestCategory("CustomerNote Tests - Read All")}
        public method GetCustomerNotes, void
        proc
            disposable data client = UnitTestEnvironment.Server.CreateClient()
            disposable data response = client.GetAsync("/odata/v1/CustomerNotes").Result
            data result = response.Content.ReadAsStringAsync().Result
            response.EnsureSuccessStatusCode()
            data customerNotes, @ODataCustomerNotes, JsonConvert.DeserializeObject<ODataCustomerNotes>(result)
            Assert.AreEqual(customerNotes.Value.Count,TestConstants.GetCustomerNotes_Count)
        endmethod

        ;;------------------------------------------------------------
        ;;Get all CustomerNotes and expand relation REL_Customer

        {TestMethod}
        {TestCategory("CustomerNote Tests - Read All")}
        public method GetCustomerNotes_Expand_REL_Customer, void
        proc
            data uri = "/odata/v1/CustomerNotes?$expand=REL_Customer"
            disposable data client = UnitTestEnvironment.Server.CreateClient()
            disposable data response = client.GetAsync(uri).Result
            data result = response.Content.ReadAsStringAsync().Result
            response.EnsureSuccessStatusCode()
        endmethod

        ;;------------------------------------------------------------
        ;;Get all CustomerNotes and expand all relations

        {TestMethod}
        {TestCategory("CustomerNote Tests - Read All")}
        public method GetCustomerNotes_Expand_All, void
        proc
            data uri = "/odata/v1/CustomerNotes?$expand=REL_Customer"
            disposable data client = UnitTestEnvironment.Server.CreateClient()
            disposable data response = client.GetAsync(uri).Result
            data result = response.Content.ReadAsStringAsync().Result
            response.EnsureSuccessStatusCode()
        endmethod

        ;;------------------------------------------------------------
        ;;Get a single CustomerNote by primary key

        {TestMethod}
        {TestCategory("CustomerNote Tests - Read by Primary Key")}
        public method GetCustomerNote, void
        proc
            disposable data client = UnitTestEnvironment.Server.CreateClient()
            data request = String.Format("/odata/v1/CustomerNotes(CustomerNumber={1})","",TestConstants.GetCustomerNote_CustomerNumber)
            data response = client.GetAsync(request).Result
            data result = response.Content.ReadAsStringAsync().Result
            response.EnsureSuccessStatusCode()
            data customerNote, @ODataCustomerNote, JsonConvert.DeserializeObject<ODataCustomerNote>(result)
        endmethod

        ;;------------------------------------------------------------
        ;;Get a single CustomerNote by primary key and expand relation REL_Customer

        {TestMethod}
        {TestCategory("CustomerNote Tests - Read by Primary Key")}
        public method GetCustomerNote_Expand_REL_Customer, void
        proc
            disposable data client = UnitTestEnvironment.Server.CreateClient()
            data request = String.Format("/odata/v1/CustomerNotes(CustomerNumber={1})?$expand=REL_Customer","",TestConstants.GetCustomerNote_Expand_REL_Customer_CustomerNumber)
            data response = client.GetAsync(request).Result
            data result = response.Content.ReadAsStringAsync().Result
            response.EnsureSuccessStatusCode()
            data customerNote, @ODataCustomerNote, JsonConvert.DeserializeObject<ODataCustomerNote>(result)
        endmethod

        ;;------------------------------------------------------------
        ;;Get a single CustomerNote by primary key and expand all relations

        {TestMethod}
        {TestCategory("CustomerNote Tests - Read by Primary Key")}
        public method GetCustomerNote_Expand_All, void
        proc
            disposable data client = UnitTestEnvironment.Server.CreateClient()
            data request = String.Format("/odata/v1/CustomerNotes(CustomerNumber={1})?$expand=REL_Customer","",TestConstants.GetCustomerNote_Expand_All_CustomerNumber)
            data response = client.GetAsync(request).Result
            data result = response.Content.ReadAsStringAsync().Result
            response.EnsureSuccessStatusCode()
            data customerNote, @ODataCustomerNote, JsonConvert.DeserializeObject<ODataCustomerNote>(result)
        endmethod

        ;;------------------------------------------------------------
        ;;Create new CustomerNote (client specified key)

        {TestMethod}
        {TestCategory("CustomerNote Tests - Create, Update & Delete")}
        public method UpdateCustomerNote, void
        proc
            disposable data client = UnitTestEnvironment.Server.CreateClient()

            ;;Get one customerNote from the file
            data getRequest = String.Format("/odata/v1/CustomerNotes({1})","",TestConstants.GetCustomerNote_CustomerNumber)
            data getResponse = client.GetAsync(getRequest).Result
            data getResult = getResponse.Content.ReadAsStringAsync().Result

            ;;Check that we got a successful response from the web service
            getResponse.EnsureSuccessStatusCode()

            ;;Deserialize the JSON into a CustomerNote object
            data doCustomerNote, @CustomerNote, JsonConvert.DeserializeObject<ODataCustomerNote>(getResult).Value[0]

            doCustomerNote.CustomerNumber = TestConstants.UpdateCustomerNote_CustomerNumber

            ;TODO: Also need to ensure any nodups alternate keys get unique values

            ;;Create new item
            disposable data requestBody = new StringContent(JsonConvert.SerializeObject(doCustomerNote),System.Text.Encoding.UTF8, "application/json")
            data request = String.Format("/odata/v1/CustomerNotes({1})","",TestConstants.UpdateCustomerNote_CustomerNumber)
            disposable data response = client.PutAsync(request, requestBody).Result

            ;;Check that we got a successful response from the web service
            response.EnsureSuccessStatusCode()

            ;;Get the inserted record
            getResponse = client.GetAsync(request).Result
            getResult = getResponse.Content.ReadAsStringAsync().Result

            ;;Check that we got a successful response from the web service
            getResponse.EnsureSuccessStatusCode()

            ;;Deserialize the JSON into a CustomerNote object
            data results = JsonConvert.DeserializeObject<ODataCustomerNote>(getResult).Value
            data resultItem, @CustomerNote
            doCustomerNote = results[0]

            ;;Test that all value PKs are the same
            foreach resultItem in results
            begin
                Assert.AreEqual(doCustomerNote.CustomerNumber, resultItem.CustomerNumber)
            end

            ;;Change the first non key field to test full update
            doCustomerNote.NoteNumber = 8

            ;;Update full item
            requestBody = new StringContent(JsonConvert.SerializeObject(doCustomerNote),System.Text.Encoding.UTF8, "application/json")
            request = String.Format("/odata/v1/CustomerNotes({1})","",TestConstants.UpdateCustomerNote_CustomerNumber)
            response = client.PutAsync(request, requestBody).Result

            ;;Check that we got a successful response from the web service
            response.EnsureSuccessStatusCode()

            ;;Get the inserted record
            getResponse = client.GetAsync(request).Result
            getResult = getResponse.Content.ReadAsStringAsync().Result

            ;;Check that we got a successful response from the web service
            getResponse.EnsureSuccessStatusCode()

            ;;Deserialize the JSON into a CustomerNote object
            doCustomerNote = JsonConvert.DeserializeObject<OdataCustomerNote>(getResult).Value[1]
            Assert.AreEqual(doCustomerNote.NoteNumber, 8)

            ;;Update one non-existant property in the customer
            data badPatchDoc = new JsonPatchDocument()
            badPatchDoc.Replace("xyzzy", "Z")

            ;;Serialize the bad patch to JSON
            data badSerializedPatch = JsonConvert.SerializeObject(badPatchDoc)

            ;;Apply the bad patch
            disposable data badPatchRequestBody = new StringContent(badSerializedPatch,System.Text.Encoding.UTF8, "application/json-patch+json")
            disposable data badPatchResponse = client.PatchAsync(request, badPatchRequestBody).Result
            ;;Check that we got a failure response from the web service
            Assert.AreEqual(badPatchResponse.StatusCode, HttpStatusCode.BadRequest)

            ;;Update one property in the customerNote
            data patchDoc = new JsonPatchDocument()
            patchDoc.Replace("NoteNumber", "9")

            ;;Serialize the patch to JSON
            data serializedPatch = JsonConvert.SerializeObject(patchDoc)

            ;;Apply the patch
            disposable data patchRequestBody = new StringContent(serializedPatch,System.Text.Encoding.UTF8, "application/json-patch+json")
            disposable data patchResponse = client.PatchAsync(request, patchRequestBody).Result

            ;;Check that we got a successful response from the web service
            patchResponse.EnsureSuccessStatusCode()

            ;;Get the updated customerNote record
            getResponse = client.GetAsync(request).Result
            getResult = getResponse.Content.ReadAsStringAsync().Result

            ;;Check that we got a successful response from the web service
            getResponse.EnsureSuccessStatusCode()

            ;;Deserialize the JSON into a CustomerNote object
            doCustomerNote = JsonConvert.DeserializeObject<OdataCustomerNote>(getResult).Value[0]

            ;;Verify that the property was changed
            Assert.AreEqual(doCustomerNote.NoteNumber, 9)

            ;;Delete It
            disposable data deleteResponse = client.DeleteAsync(request).Result

            ;;Check that we got a successful response from the web service
            getResponse.EnsureSuccessStatusCode()

            ;;Attempt to get the deleted record
            getResponse = client.GetAsync(request).Result

            ;;Check we got a fail state from the web service
            Assert.AreEqual(getResponse.IsSuccessStatusCode, true)

        endmethod

    endclass

endnamespace
